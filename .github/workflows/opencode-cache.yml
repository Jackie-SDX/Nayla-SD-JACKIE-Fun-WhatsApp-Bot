name: opencode-cache

on:
  push:
    branches: [main]
  workflow_dispatch:
  schedule:
    - cron: '17 0 * * *'

concurrency:
  group: opencode-cache
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  warm:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Verify pinned OpenCode V2 package
        id: runtime
        env:
          OPENCODE_V2_VERSION: "2.0.3"
        shell: bash
        run: |
          set -euo pipefail
          npm view "@opencode/cli@${OPENCODE_V2_VERSION}" version --json | jq -e --arg v "$OPENCODE_V2_VERSION" '. == $v' >/dev/null
          echo "Verified OpenCode V2 package: $OPENCODE_V2_VERSION"

      - name: Restore OpenCode V2 cache
        id: cache_restore
        continue-on-error: true
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: ~/.opencode
          key: opencode-${{ runner.os }}-${{ runner.arch }}-2.0.3

      - name: Install OpenCode V2 on cache miss
        if: steps.cache_restore.outputs.cache-hit != 'true'
        env:
          OPENCODE_V2_VERSION: "2.0.3"
        shell: bash
        run: |
          set -euo pipefail
          rm -rf "$HOME/.opencode"
          npm install --prefix "$HOME/.opencode" --no-audit --no-fund --prefer-online --save-exact "@opencode/cli@${OPENCODE_V2_VERSION}"
          test -x "$HOME/.opencode/bin/opencode"
          actual="$("$HOME/.opencode/bin/opencode" --version)"
          echo "OpenCode V2 runtime: $actual"
          [[ "$actual" == *"$OPENCODE_V2_VERSION"* ]]

      - name: Save verified OpenCode V2 cache
        if: steps.cache_restore.outputs.cache-hit != 'true'
        continue-on-error: true
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: ~/.opencode
          key: opencode-${{ runner.os }}-${{ runner.arch }}-2.0.3

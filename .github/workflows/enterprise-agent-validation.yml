name: enterprise-agent-validation

on:
  push:
    branches:
      - 'audit/**'
      - 'feature/**'
      - 'fix/**'
      - 'oc/**'
  pull_request:
  workflow_dispatch:

concurrency:
  group: enterprise-agent-validation-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
